# SugarDisableFavoriteFollows

* Clone the repository.
* Run: `composer install` to retrieve the sugar-module-packager dependency.
* Generate the installable .zip Sugar module with: `./vendor/bin/package <version number>`.
